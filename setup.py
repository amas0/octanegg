from setuptools import setup, find_packages

setup(name='octane-py',
      version='0.1',
      author='amas0',
      description='Python client library for octane.gg public API',
      install_requires=[
            'requests'
      ])
